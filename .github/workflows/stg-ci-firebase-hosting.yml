# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge with staging
'on':
  push:
    branches:
      - staging
env:
  PROJECT_ID: "blueseed-ecom"
  API_URL: "http://staging-blueseedapi-159133186.us-east-1.elb.amazonaws.com/api/ecommerce"

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Make envfile
        uses: SpicyPizza/create-envfile@v1
        with:
          envkey_REACT_APP_API_URL: ${{ env.API_URL }}
          file_name: .env
      - run: npm i
      - run: npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_BLUESEED_ECOM }}'
          channelId: live
          target: stg
          projectId: ${{ env.PROJECT_ID }}